import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'moodbored.app.01',
    appName: 'MoodBored',
    webDir: 'dist/mood-bored/browser'
};

export default config;
